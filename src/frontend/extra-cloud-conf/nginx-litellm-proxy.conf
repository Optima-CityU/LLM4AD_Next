# 禁止外部访问 LiteLLM UI/API，避免绕过 gateway 鉴权。
location = /litellm {
    return 403;
}

location ^~ /litellm/ {
    return 403;
}

# 防御性拒绝 /litellm_proxy 直接访问（防止未来配置错误）
location ^~ /litellm_proxy/ {
    return 403;
}
